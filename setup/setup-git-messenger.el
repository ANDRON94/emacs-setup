(use-package git-messenger
  :ensure t
  :bind (("C-c d m" . git-messenger:popup-message))
                                        ;:map git-messenger-map - Default keybindings is pretty good
                                        ;())
  :config)
                                        ;()) Default config is pretty good =)
